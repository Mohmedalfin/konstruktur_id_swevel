const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch({headless: 'new'}); const page = await browser.newPage(); await page.goto('http://localhost/konstruktor.id/public/test-swal.html'); await page.waitForSelector('.swal2-deny'); const text = await page.$eval('.swal2-deny', el => el.textContent); console.log('DENY TEXT:', text); await browser.close(); })();